# Discord Developer Portal で作った Bot トークン
DISCORD_TOKEN=ここにトークンを貼る

# 任意。入れると通常コマンドがそのサーバーへすぐ同期されます。
# 複数サーバーで使う場合は空のままにします。
GUILD_ID=

# ボット運用者専用コマンド（/owner）を出すサーバー。
# 空のときは、運用者が /owner bind で今いるサーバーに一度だけ固定できます。
# 入っている場合はコマンドでは変更できず、このファイルを直接書き換えて再起動します。
OWNER_GUILD_ID=
